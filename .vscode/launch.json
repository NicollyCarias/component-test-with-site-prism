{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Cucumber Site Prism",
            "type": "Ruby",
            "request": "launch",
            "preLaunchTask": "install",
            "useBundler": true,
            "cwd": "${workspaceRoot}",
            "program": "${workspaceRoot}/bin/cucumber"
        }
    ]
  }